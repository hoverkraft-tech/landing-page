---
export interface Props {
  id: string;
}

const { id } = Astro.props;
---

<div class="mx-auto mt-4 w-full max-w-[420px]">
  <svg data-radar viewBox="0 0 500 500" class="h-auto w-full text-secondary" role="img" aria-label="Radar de maturité">
    <defs>
      <linearGradient id={`${id}-polyGradient`} x1="0" x2="0" y1="0" y2="1">
        <stop offset="0%" stop-color="currentColor" stop-opacity="0.55"></stop>
        <stop offset="100%" stop-color="currentColor" stop-opacity="0.15"></stop>
      </linearGradient>
    </defs>
  </svg>
</div>
