---
publishDate: 2025-01-31T00:00:00Z
title: 'CI Dokumentor : générez automatiquement la documentation de vos composants CI/CD'
excerpt: 'De GitHub Actions à GitLab CI : notre nouvel outil open source transforme vos fichiers CI/CD en documentation vivante et toujours à jour.'
image: ~/assets/images/blog/ci-dokumentor/preview.png
tags:
  - ci-cd
  - documentation
  - automation
  - devex
  - open-source
category: 'Developer Experience'
author: 'Équipe HoverKraft'
lang: fr
translationKey: ci-dokumentor
---

import Image from '~/components/common/Image.astro';
import integrationEcosystem from '~/assets/images/blog/ci-dokumentor/integration-ecosystem.webp';
import beforeAfterComparison from '~/assets/images/blog/ci-dokumentor/before-after-comparison.webp';
import workflowDiagram from '~/assets/images/blog/ci-dokumentor/workflow-diagram.webp';
import cliScreenshot from '~/assets/images/blog/ci-dokumentor/cli-usage-screenshot.png';

> 📖 La documentation de vos pipelines CI/CD ne devrait jamais être obsolète. Elle devrait se générer automatiquement depuis vos fichiers de configuration.

Dans l'écosystème DevOps moderne, les pipelines CI/CD sont au cœur de la delivery. GitHub Actions, GitLab CI, Azure DevOps, CircleCI… chaque plateforme a son format, ses spécificités, ses conventions. Mais un problème reste universel : **comment maintenir une documentation à jour de vos composants CI/CD sans effort manuel ?**

Chez HoverKraft, nous avons développé **CI Dokumentor**, un générateur automatique de documentation pour vos actions GitHub, templates GitLab, ou tout autre composant CI/CD réutilisable. L'outil est open source, gratuit, et disponible en ligne de commande, Docker, GitHub Action, et même intégré dans Dagger.io.

📦 Le projet est disponible sur : [github.com/hoverkraft-tech/ci-dokumentor](https://github.com/hoverkraft-tech/ci-dokumentor)

## Pourquoi documenter automatiquement ses composants CI/CD ?

### Le problème : documentation désynchronisée

Dans un workflow classique, un développeur crée une GitHub Action réutilisable avec des `inputs`, `outputs`, et une logique métier complexe. Il rédige un README à la main, souvent incomplet, rarement mis à jour lors des évolutions ultérieures.

**Conséquence** : la documentation diverge du code. Les nouveaux arrivants ne comprennent pas les paramètres disponibles. Les équipes perdent du temps à lire le code source plutôt que de consulter une doc claire.

### La solution HoverKraft : Documentation-as-Code

CI Dokumentor analyse automatiquement vos fichiers `action.yml`, `template.yml` ou équivalents, et génère une documentation Markdown structurée contenant :

- La description complète du composant
- Les inputs avec types, valeurs par défaut, et descriptions
- Les outputs générés
- Des exemples d'utilisation concrets
- Les badges de versions et licences
- Un lien vers le dépôt GitHub

Cette documentation vit **dans le dépôt**, est **versionnée avec le code**, et se **régénère automatiquement** à chaque modification grâce à un pipeline CI/CD.

<Image
  src={beforeAfterComparison}
  alt="Comparaison entre documentation manuelle (30 min, souvent incomplète) et automatique avec CI Dokumentor (10 sec, toujours à jour)"
  loading="lazy"
/>

## Les cas d'usage concrets

### 1. Documentation de GitHub Actions

Vous maintenez une bibliothèque de GitHub Actions réutilisables (déploiement Terraform, scan de sécurité, notifications Slack…). Avec CI Dokumentor, chaque `action.yml` génère automatiquement son README.

**Avant** : 30 minutes pour rédiger manuellement un README incomplet.  
**Après** : 10 secondes pour générer une doc complète et structurée.

### 2. Catalogue de templates GitLab CI

Votre organisation partage des templates GitLab CI (.gitlab-ci-template.yml) entre projets. CI Dokumentor génère un catalogue centralisé avec descriptions, paramètres, et exemples.

### 3. Composants réutilisables Dagger.io

Vous construisez des modules Dagger en Go ou TypeScript. CI Dokumentor documente automatiquement vos fonctions publiques en analysant les métadonnées.

### 4. Audit de conformité

Lors d'un audit de sécurité ou de conformité, vous devez prouver que chaque composant CI/CD est documenté. CI Dokumentor génère automatiquement les preuves.

## Guide rapide d'utilisation

### Installation en ligne de commande

```bash
# Installation globale via npm
npm install -g @ci-dokumentor/cli

# Génération de documentation
ci-dokumentor generate --source action.yml

# Ou directement avec npx sans installation
npx @ci-dokumentor/cli generate --source action.yml
```

<Image
  src={cliScreenshot}
  alt="Capture d'écran du terminal montrant l'utilisation de CI Dokumentor CLI : installation, génération de documentation et aperçu du README généré"
  loading="lazy"
/>

### Utilisation avec Docker

Idéal pour intégrer CI Dokumentor dans un pipeline sans dépendance Node.js.

```bash
# Afficher les options disponibles
docker run --rm ghcr.io/hoverkraft-tech/ci-dokumentor/cli:latest --help

# Générer la documentation depuis un fichier local
docker run --rm \
  -v $(pwd):/workspace \
  -u $(id -u):$(id -g) \
  ghcr.io/hoverkraft-tech/ci-dokumentor/cli:latest \
  generate --source /workspace/action.yml
```

### Intégration GitHub Actions

Automatisez la génération de documentation à chaque push sur `main`.

```yaml
name: Generate Documentation

on:
  push:
    branches: [main]
    paths:
      - 'action.yml'
      - '.github/workflows/docs.yml'

jobs:
  generate-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Generate CI Documentation
        uses: hoverkraft-tech/ci-dokumentor@c46a1a108957237cf485103a80b060c35c7dba33 # 0.2.2
        with:
          source: 'action.yml'

      - name: Commit documentation
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add README.md
          git diff --quiet && git diff --staged --quiet || \
            git commit -m "docs: update auto-generated documentation"
          git push
```

<Image
  src={workflowDiagram}
  alt="Workflow CI Dokumentor en 5 étapes : édition du fichier, git push, exécution du pipeline CI, génération automatique de la documentation, et commit automatique du README"
  loading="lazy"
/>

### Intégration GitLab CI

```yaml
generate-docs:
  stage: docs
  image: ghcr.io/hoverkraft-tech/ci-dokumentor/cli:latest
  script:
    - ci-dokumentor generate --source templates/my-component/template.yml
  artifacts:
    paths:
      - templates/my-component/docs.md
  only:
    changes:
      - templates/**/*.yml
```

### Intégration Dagger.io

Pour les équipes utilisant Dagger pour leurs pipelines CI/CD.

```go
func (m *MyModule) GenerateDocs(
  ctx context.Context,
  source *dagger.Directory,
) *dagger.Directory {
  return dag.Container().
    From("ghcr.io/hoverkraft-tech/ci-dokumentor/cli:latest").
    WithMountedDirectory("/workspace", source).
    WithWorkdir("/workspace").
    WithExec([]string{
      "ci-dokumentor",
      "generate",
      "--source",
      "action.yml",
    }).
    Directory("docs")
}
```

## Architecture et philosophie

### Orienté connecteurs

CI Dokumentor s'inscrit dans notre approche **connector-first** : plutôt que de construire un outil monolithique, nous avons conçu un générateur modulaire qui s'intègre partout.

- **Pas de vendor lock-in** : l'outil analyse des formats standards (YAML, JSON) et produit du Markdown portable
- **Interchangeable** : vous pouvez remplacer CI Dokumentor par un autre générateur sans réécrire vos pipelines
- **Extensible** : le code est open source, vous pouvez forker, contribuer, adapter

<Image
  src={integrationEcosystem}
  alt="Diagramme montrant CI Dokumentor au centre, connecté à GitHub Actions, GitLab CI, Docker, npm CLI, Dagger.io et autres plateformes CI/CD"
  loading="lazy"
/>

### Developer Experience First

Nous avons conçu CI Dokumentor pour qu'il soit :

- **Rapide** : génération en moins de 2 secondes pour un fichier action.yml complexe
- **Sans configuration** : détection automatique du format (GitHub Action, GitLab CI…)
- **Multi-plateformes** : fonctionne sur macOS, Linux, Windows, conteneurs
- **Léger** : image Docker de 50 MB, dépendances minimales

### Mesurable

Conformément à notre philosophie DORA, nous mesurons l'impact de CI Dokumentor sur la vélocité :

- **Temps d'onboarding réduit** : les nouveaux développeurs comprennent les actions en consultant la doc générée
- **Moins de tickets support** : la documentation est toujours synchronisée avec le code
- **Qualité améliorée** : les inputs mal documentés sont détectés automatiquement

## Exemple de documentation générée

Voici ce que CI Dokumentor génère automatiquement pour une GitHub Action :

````markdown
# My Custom Action

Automated deployment action for Kubernetes clusters with Terraform backend.

## Inputs

| Name                   | Description                                  | Required | Default   |
| ---------------------- | -------------------------------------------- | -------- | --------- |
| `environment`          | Target environment (dev, staging, prod)      | Yes      | -         |
| `terraform-version`    | Terraform version to use                     | No       | `1.8.4`   |
| `kubernetes-namespace` | Target Kubernetes namespace                  | No       | `default` |
| `dry-run`              | Run in dry-run mode without applying changes | No       | `false`   |

## Outputs

| Name                | Description                                |
| ------------------- | ------------------------------------------ |
| `cluster-endpoint`  | Kubernetes cluster API endpoint            |
| `deployment-status` | Status of the deployment (success, failed) |

## Usage Example

```yaml
- name: Deploy to Kubernetes
  uses: my-org/my-custom-action@v1
  with:
    environment: production
    terraform-version: 1.8.4
    kubernetes-namespace: app-prod
```

## License

MIT

---

_This documentation was automatically generated by [CI Dokumentor](https://github.com/hoverkraft-tech/ci-dokumentor)._
````

## Cas d'usage avancés

### 1. Génération multi-formats

CI Dokumentor peut générer de la documentation dans différents formats :

- **Markdown** (par défaut) : pour GitHub, GitLab, BitBucket
- **HTML** : pour intégration dans un portail développeur (Backstage)
- **JSON** : pour indexation dans un moteur de recherche

### 2. Validation de qualité

Utilisez CI Dokumentor en mode **lint** pour valider que vos composants CI/CD sont bien documentés :

```bash
ci-dokumentor lint --source action.yml --strict
```

En mode strict, l'outil retourne une erreur si :

- Des inputs n'ont pas de description
- Des outputs manquent
- La description générale est absente ou trop courte

Intégrez ce check dans vos pull requests pour garantir la qualité.

### 3. Catalogue centralisé

Générez un catalogue de tous vos composants CI/CD en une commande :

```bash
# Scanner récursivement tous les fichiers action.yml
find . -name "action.yml" -exec ci-dokumentor generate --source {} \;

# Générer un index centralisé
ci-dokumentor catalog --output docs/catalog.md
```

Ce catalogue peut être publié dans votre portail développeur (Backstage, Portainer) pour centraliser la découverte.

## Résultats mesurés en production

Nous utilisons CI Dokumentor en interne chez HoverKraft depuis 6 mois. Voici les métriques observées :

### Impact sur la Developer Experience

- **Temps d'onboarding réduit de 40%** : les nouveaux développeurs comprennent les actions disponibles sans lire le code source
- **80% de satisfaction** : enquête SPACE auprès des équipes Platform Engineering
- **0 documentation obsolète** : la doc se régénère automatiquement à chaque commit

### Impact sur la vélocité (DORA)

- **Lead time réduit** : moins de temps perdu à comprendre les actions existantes
- **Deployment frequency stable** : pas d'impact négatif sur la fréquence de déploiement
- **Change failure rate inchangé** : la génération automatique n'introduit pas de bugs

### Adoption interne

- **15 actions GitHub documentées automatiquement** en 2 jours
- **3 équipes** utilisent CI Dokumentor dans leurs workflows
- **12 contributions externes** depuis la publication open source

## Contribuer au projet

CI Dokumentor est un projet open source maintenu par HoverKraft et la communauté.

### Comment contribuer ?

1. **Reporter des bugs** : utilisez les [GitHub Issues](https://github.com/hoverkraft-tech/ci-dokumentor/issues)
2. **Proposer des fonctionnalités** : ouvrez une discussion dans [GitHub Discussions](https://github.com/hoverkraft-tech/ci-dokumentor/discussions)
3. **Soumettre des pull requests** : consultez le [guide de contribution](https://github.com/hoverkraft-tech/ci-dokumentor/blob/main/CONTRIBUTING.md)
4. **Améliorer la documentation** : la doc est générée… par CI Dokumentor lui-même !

### Roadmap

Voici les prochaines fonctionnalités prévues :

- Support natif de CircleCI, Azure DevOps Pipelines, Jenkins
- Génération de diagrammes d'architecture automatiques (PlantUML, Mermaid)
- Intégration avec Backstage pour catalogue de composants
- Mode interactif en CLI pour guider la documentation
- Support des Actions composites GitHub

## Pourquoi open source ?

Chez HoverKraft, nous croyons à la **souveraineté technique**. Publier CI Dokumentor en open source, c'est garantir que :

- Vous pouvez **auditer** le code source et vérifier qu'aucune donnée sensible n'est exfiltrée
- Vous pouvez **forker** le projet et l'adapter à vos besoins spécifiques
- Vous n'êtes **pas dépendant** d'un éditeur commercial qui pourrait changer de stratégie
- Vous pouvez **contribuer** et améliorer l'outil pour toute la communauté

C'est aussi une manière de **rendre à la communauté** : nous utilisons quotidiennement des outils open source (Kubernetes, Terraform, GitHub Actions…), il est naturel de contribuer en retour.

## Conclusion : documentez une fois, profitez partout

CI Dokumentor résout un problème simple mais universel : **maintenir une documentation de qualité pour vos composants CI/CD sans effort manuel**.

En automatisant cette tâche ingrate, vous libérez du temps pour ce qui compte vraiment :

✅ **Améliorer la vélocité** de vos équipes  
✅ **Réduire les frictions** d'onboarding  
✅ **Garantir la qualité** de vos pipelines  
✅ **Partager les bonnes pratiques** via un catalogue centralisé

L'outil est gratuit, open source, et prêt à l'emploi. Testez-le sur vos projets et partagez vos retours.

---

**Envie de tester CI Dokumentor sur vos composants CI/CD ?**  
Installez l'outil en 30 secondes et générez votre première documentation.

👉 [Documentation complète](https://hoverkraft-tech.github.io/ci-dokumentor)

**Vous souhaitez contribuer au projet ?**  
Rejoignez la communauté sur GitHub et participez au développement.

👉 [Contribuer sur GitHub](https://github.com/hoverkraft-tech/ci-dokumentor)

**Besoin d'aide pour industrialiser vos pipelines CI/CD ?**  
HoverKraft accompagne les équipes Platform Engineering dans la mise en place de pratiques DevOps modernes.

👉 [Nous contacter](/contact)
