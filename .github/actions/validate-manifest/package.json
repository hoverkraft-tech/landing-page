{
  "name": "validate-branding-manifest",
  "version": "1.0.0",
  "private": true,
  "description": "Validates branding manifest inputs for workflows",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "test:ci": "node --test"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "mock-fs": "^5.4.0"
  }
}
