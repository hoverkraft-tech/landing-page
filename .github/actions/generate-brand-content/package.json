{
  "name": "generate-brand-content",
  "version": "1.0.0",
  "private": true,
  "description": "Generates TypeScript files for brand colors, mission, typography, and logos",
  "main": "index.js",
  "scripts": {
    "test": "node --test",
    "test:ci": "node --test"
  },
  "devDependencies": {
    "mock-fs": "^5.4.0"
  }
}
